version: '2'
services:
  mhacks_db:
    extends:
      file: docker-compose.yml
      service: mhacks_db
  mhacks_website:
    extends:
      file: docker-compose.yml
      service: mhacks_website
    ports:
      - "8080:8080"
    volumes:
      - ..:/usr/src/app
    links:
      - mhacks_db